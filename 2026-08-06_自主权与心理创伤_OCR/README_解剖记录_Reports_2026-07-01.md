# 《自主权与心理创伤.pdf》解剖记录（2026-08-06）

## 结论：此 PDF 不是书
- 258 页中仅 4 页有内容（page 93/112/113/131），其余 254 页为空壳（无文本层/无图像/无绘制指令，渲染全白）
- 全部内容 = 5 张嵌入图片（见 imgs/）：4 张 AI 少女形象图 + 1 张 408 数据结构笔记截图
- 无文字可 OCR，无《自主权与心理创伤》内容——文件名与内容不匹配（疑似下载损坏/张冠李戴）
- 用户决定：跳过此 PDF

## 诊断方法（可复用）
1. `page.get_images(full=True)` + `page.read_contents()` 检查页内是否有图像/内容流
2. `doc.xref_get_key(i, 'Type')` 遍历对象类型，统计 Image XObject
3. 文件字节级检查：JPEG 魔数 (FFD8FF)、DCTDecode/FlateDecode 标记
4. 渲染测试：200 DPI 灰度渲染后查非白像素比例（全白 = 页面无内容）
