# Agent 记忆层（AI Memory Layer）竞品调研合成报告

> 日期：2026-08-29
> 调研目标：评估"KOS 项目化 → 卖施工长出来的工具（Agent 记忆/质量审计组件）"这条商业化路径的竞争环境
> 方法：GitHub API 多关键词搜索（8+6 组）→ 浅克隆 4 项目解剖（EverOS/basic-memory/mem0/letta）→ 3 路并行 subagent 解剖 + Hermes 核心文件交叉验证 → 代理网络补融资情报
> 结论一句话：**赛道已红海化（头号玩家 64k★），但"记忆质量审计/防漂移/可信度"这个垂直缝隙无人占领——KOS 执行层自动化恰好落在这个缝隙上**

---

## 一、赛道全景（2026-08 实测）

### 1.1 玩家分层

| 梯队 | 项目 | Stars | 定位 | 备注 |
|------|------|-------|------|------|
| 头部 | mem0ai/mem0 | **64.3k** | Universal memory layer for AI Agents | YC S24，10 人，SF，Apache-2.0 |
| 头部 | TencentDB-Agent-Memory | 25.1k | 团队级记忆中枢 | **腾讯入场**，local-first 标签 |
| 头部 | topoteretes/cognee | 30.3k | AI memory platform（图+向量） | cognee.ai 商业化 |
| 头部 | letta-ai/letta (MemGPT) | 24.5k | stateful agents 平台 | 主线已迁 letta-code (TS) |
| 第二梯队 | EverMind-AI/EverOS | 12.5k | **local-first + Markdown-native** 记忆层 | 与 KOS 理念最近 |
| 第二梯队 | memvid/memvid | 16.5k | serverless 记忆层 | Rust |
| 第二梯队 | MemoriLabs/Memori | 16.3k | agent-native 记忆基础设施 | |
| 第三梯队 | basicmachines-co/basic-memory | 3.8k | MCP server 记忆 + **云托管 $15/月** | AGPL，Obsidian 生态 |
| 第三梯队 | getzep/zep | 4.9k | 长期记忆 | |
| 第三梯队 | langchain-ai/langmem | 1.6k | LangChain 系记忆 | |
| 垂直 | DeusData/codebase-memory-mcp | 41.0k | **代码库**记忆图谱 | C 语言，代码智能 |
| 垂直 | OSU-NLP-Group/HippoRAG | 4.0k | 学术 RAG 长期记忆 | NeurIPS'24 |
| 垂直 | Gentleman-Programming/engram | 6.2k | 编码 Agent 持久记忆 | Go |
| 长尾 | iwe-org/iwe / swarmclawai/swarmvault / my-neuro / AionsHome | <1.6k | Markdown 图谱 / AI 伴侣记忆 | |

### 1.2 关键结构性发现

1. **腾讯入局**（25k★ 团队级记忆中枢）= 大厂已确认赛道价值，同时也意味着纯"记忆存储"方向的创业窗口在关闭——你不可能跟腾讯打存储
2. **codebase-memory-mcp 41k★** = "记忆"赛道里最垂直（代码库）的玩家反而最猛——**垂直化是生存策略的实证**
3. **AI 伴侣记忆方向存在但稀疏**（my-neuro 1.3k / AionsHome 754 / awesome-ai-companion 571）——与你永月方向直接相关，但商业化远未成熟

---

## 二、核心玩家解剖（代码级证据）

### 2.1 EverOS（12.5k★，local-first + Markdown-native）—— 理念最接近 KOS 的竞品

**存储三件套**（docs/how-memory-works.md + 源码实证）：
- **Markdown + YAML frontmatter = 唯一真相源**（`~/.everos/{users,agents,knowledge}/`，episode 按日追加）
- SQLite（.index/sqlite/）= 状态/审计/变更队列/LSN
- LanceDB（.index/lancedb/）= 向量 + BM25 + scalar 索引，**可整体从 md 重建**——"Delete the entire .index/ directory and no memory is lost"

**与 KOS 的哲学惊人相似**：md 为真相源、索引可重建、frontmatter 承载状态、episode 按日追加（=KOS 的不可变事件源）。

**🔴 关键交叉验证**：EverOS 的 "audit-form" **不是记忆质量审计机制**——它是条目解析格式（`<!-- entry:xxx -->` marker + H2 header + `**key**: value` + H3 sections 的结构化解析），名字叫 audit 但功能是"便于审计的格式"，**没有校验、冲突检测、可信度评级**。差异化缝隙确认存在。

**补充（subagent 代码级证据）**：EverOS 有两个近似机制但都不到质量审计——
- **cascade/reconciler**：用户直接编辑 md → watcher 扫 mtime → 对比 sqlite 增量重建索引（防的是"索引漂移"，LanceDB 可整体重建）
- **Reflection（OME，默认周一 2 点）**：Select→Merge→Re-extract→Deprecate 聚类合并 episodes、deprecated 旧文件（防的是"记忆碎片噪声"）
- 结论：**防的是索引漂移与碎片，非事实真伪校验**——"内容事实层审计"仍然是空白。

**商业模式**：开源生态（EverMind 公司背书，HuggingFace 账号、WeCom 社区），persona engine（"构建有感受的 AI"，灵感来自 Her——与永月方向正面撞车）。

### 2.2 Basic Memory（3.8k★，MCP server + 云托管）

- 存储：**Markdown 文件 = 唯一事实源**（NOTE-FORMAT.md: "Files are the source of truth"）+ SQLite 索引（FTS5 全文 + sqlite-vec 向量），可选 pgvector/Milvus
- 知识图谱：Entity → Observations（`- [category] text #tag`）/ Relations（`- type [[Entity]]`）——三元素语法
- 写入：MCP 工具层（write_note）→ Markdown → indexing 流水线自动更新图
- **商业模式最清晰**：云托管 **$15/月锁价**（12.5/月年付）+ Basic Memory Teams（团队共享）+ 自托管免费 + AGPL
- **质量机制（subagent 代码级证据）**：引擎层有"一致性维护"无"语义审计"——note_content_reconciler.py（DB↔文件漂移修复/read repair）、orphan_cleanup.py（清孤儿索引）、forward_reference_resolution.py（先引后建）、hooks/envelope.py（幂等去重）；**写路径无内容校验**。语义质量靠 prompt 层 4 个 skills（memory-curate 找孤儿/提议关系/合并重复、memory-defrag 拆大文件/去重/清 stale、memory-reflect 回顾整合、memory-capture 同线程重写）——**质量责任外包给 Agent prompt，引擎无门禁 = 语义质量审计空白缝隙**

### 2.3 Mem0（64.3k★，赛道头号）—— V3 架构大转向

**🔴 关键发现（subagent + 源码交叉验证）**：Mem0 当前版本已切换到 **V3 "ADD-only 追加式"算法**（2026-04 公告）：
- **不再自动触发 UPDATE/DELETE**——旧版自动"更新过期记忆"机制被放弃，改纯追加
- **graph 存储层已从 OSS SDK 移除**（被实体链接替代）
- **semantic/episodic memory 官方文档明确"未实现"**
- 核心机制：LLM 从对话流抽取"记忆"（事实/偏好）而非存原文 → 跨会话个性化
- 质量机制（subagent 代码级证据）：①MD5 哈希去重（批内+批间双去重）②**变更审计 = SQLite history 表 + history(memory_id) API**——每次 ADD/UPDATE/DELETE 可回溯（但这是**操作日志审计**，非内容质量审计）③无显式冲突检测（V3 放弃 UPDATE 语义后"靠只增不改+时间戳回避"）④confidence 字段仅 platform 元数据可选，OSS 不强制。**结论：有"操作日志"，无"质量审计"——无冲突检测、无置信度评分、无运行时质量校验闭环**
- V3 七阶段流水线（add()→_add_to_vector_store L879）：最近10条消息上下文 → embed+top_k=10 现有记忆 → 单次 LLM 调用抽取新事实（ADDITIVE_EXTRACTION_PROMPT）→ 批量 embed → MD5 去重 → 批量写向量库+SQLite history → 实体提取与跨记忆链接
- 商业模式：Library（免费）/ Self-Hosted Server / **Cloud Platform（managed）**三档 + agent 自主注册（`mem0 init --email`）
- 团队：YC S24，10 人，SF

**解读**：Mem0 从"主动管理记忆"退到"纯追加存储"——说明"自动记忆更新"这个方向连头号玩家都做不动（LLM 判断何时更新记忆=不可靠）。**这是 KOS 最大的机会信号**：大家都放弃了"记忆质量把关"，因为太难；但 KOS 的整层架构就是为"把关"设计的（校验器/验收/评级）。

### 2.4 Letta / MemGPT（24.5k★）

- 定位：stateful agents（"like people, with memory, identity, learn and adapt"）
- 主线已迁 letta-code（TS，3.1k★）——原 Python 仓库成门面
- 记忆架构：core memory + archival + recall（MemGPT 经典三层，虚拟上下文管理）
- 商业化：Letta Cloud（跨设备同步 agent 记忆/身份/对话）
- **注意**：定位文案"agents that are like people" = 与永月（AI 伴侣）方向正面重叠——它做的是"agent 的人格化记忆基础设施"

---

## 三、差异化缝隙分析（KOS 路线 C 的战场）

### 3.1 竞品全部缺失的能力矩阵

| 能力 | Mem0 | EverOS | BasicMemory | Letta | KOS（设计） |
|------|------|--------|-------------|-------|------------|
| 记忆存储 | ✅ 向量+graph | ✅ md+向量 | ✅ md+向量 | ✅ 分层 | ✅ md 主库 |
| 跨会话个性化 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 来源可追溯（溯源链） | ❌ | ⚠️ frontmatter 有 | ❌ | ❌ | ✅ **铁律** |
| 记忆质量审计/校验 | ❌ 仅去重 | ❌ audit-form 只是格式 | ❌ | ❌ | ✅ **校验器关卡** |
| 冲突检测 | ❌ | ❌ | ❌ | ❌ | ✅ 对辩/矛盾记录 |
| 置信度/诚实分级 | ❌ | ❌ | ❌ | ❌ | ✅ HML/SG1-5 |
| 防漂移（schema 一致性） | ❌ | ❌ | ❌ | ❌ | ✅ 漂移扫描器 |
| 第一人称认知演化 | ❌ | ❌ | ❌ | ❌ | ✅ Self 层演化史 |
| 反身性监控（苗床） | ❌ | ❌ | ❌ | ❌ | ✅ 设计定稿 |

**结论：KOS 的整层差异化 = 记忆的"质量保证层"（QA for AI memory），而所有竞品都只做"存储+检索"，没人做"防错+可审计"。** 这与 Mem0 V3 放弃自动更新形成完美对照：**头号玩家撤退的战场，正是 KOS 设计最厚的战场。**

### 3.1.5 "审计"一词的三层区分（🔴 本调研最重要的精化）

三个 subagent 解剖 + 交叉验证后，竞品口中的 "audit" 其实是三个不同层次，KOS 必须精确占位：

| 层次 | 含义 | 谁有 | KOS |
|------|------|------|-----|
| **操作日志审计** | 记录每次 ADD/UPDATE/DELETE 事件，可回溯（`history(memory_id)` API） | ✅ Mem0（SQLite history） | 有（git + update_log） |
| **一致性维护** | 文件↔索引漂移修复、孤儿清理、幂等去重（read repair） | ✅ EverOS（cascade/reconciler）✅ BasicMemory（reconciler/orphan） | 有（漂移扫描器） |
| **语义质量审计** | 内容事实真伪、冲突检测、置信度分级、防知识腐化 | ❌ **全部竞品空白** | ✅ KOS 设计（校验器/对辩/HML/SG1-5/苗床） |

**结论升级**：前两层（操作日志、一致性）是"工程审计"，竞品已有且不难做——**KOS 不能拿这两层当差异化卖点**。真正的缝隙在第三层：**语义质量审计**——"记了什么"之后"记得对不对、有没有自相矛盾、置信度多高、有没有腐化"。这正是 Mem0 V3 撤退的原因（LLM 把关不可靠）也是 KOS 从第一天就在用纪律对抗的东西。

### 3.2 但要诚实面对的竞争风险

1. **EverOS 理念撞车**：md 真相源+可重建索引+frontmatter 状态——EverOS 已经实现 KOS 的"markdown 管写、数据库管查"理念。**不能宣传"md 优先"为差异化（EverOS 已占），只能宣传"质量保证"为差异化**
2. **大厂入场**：腾讯 25k★ 团队记忆中枢——"记忆存储"基建市场会像向量库一样被云厂商吞掉
3. **无竞品占领 ≠ 有市场需求**：质量审计无人做，可能是因为市场还没痛到要买——需要验证（下面给验证方法）
4. **Basic Memory 商业化先行**：$15/月云托管已跑通收费——证明"AI 记忆工具"有付费意愿，但它是通用工具打法

### 3.3 KOS 路线 C 的可行形态（结合全部证据）

不做"又一个记忆层"，做**"Agent 记忆的可信度层"**：

```
Agent 记忆（Mem0/EverOS/basic-memory 管存储）
        ↓ 接入
KOS Audit Layer（校验器/溯源/冲突/置信度/漂移扫描）
        ↓
可信记忆（可审计、可追溯、可评级）
```

**最小可行产品候选**（按 P0 排序，前面已定）：
1. 漂移扫描器（只读，全库 frontmatter/命名/断链/枚举一致性报告）——**零风险 MVP，立即做**
2. 符号-表述一致性检查器（静态规则）——EGS 论文已证明痛点（α bug/方向写反）
3. 卡 schema 校验器（pydantic 卡型定义）——编码化思路 v0.1 已设计

---

## 四、商业化路径判断（更新版）

| 路径 | 确定性 | 证据 | 判断 |
|------|--------|------|------|
| A. 求职杠杆（2027.01 投 Agent 开发） | 最高 | EGS 兑现日志 + 本调研 + 组件实物 | **主战场，不变** |
| C. 记忆审计组件 | 中高 | 本调研：缝隙真实存在，头号玩家撤退 | **副产物顺手积累**，不单独立项 |
| B. 方法论内容（EGS/B站/知乎） | 中 | 既有产线 | 延长线，不主动 |
| 〇. 卖系统本身 | ❌ | 红海 + 腾讯入场 | 永不 |

**关键判断更新**：本调研没有改变路径排序，但**加强了 C 的证据**——"质量审计缝隙无人占领 + Mem0 V3 撤退"是比直觉更硬的市场信号。同时确认了**禁止宣传"md 优先/本地优先"为差异化**（EverOS 已占位），差异化只打"可信度/审计/防漂移"。

---

## 五、下一步建议

1. **立即做 P0 漂移扫描器**（只读、零风险、既是内部工具又是 C 路径 MVP 原型）
2. **写卡裁决四件事**（F7/基石层/演化层枚举/苗床进包）——编码前的清障
3. **缝隙验证实验**（3 个月内）：把 KOS 巡检报告（漂移率/冲突数/溯源断链数）包装成"AI 记忆健康报告"样例——拿去问 2-3 个做 Agent 产品的人"这个你们需要吗"，用真实反馈验证市场，不猜

---

## 六、信息源

| 来源 | 用途 |
|------|------|
| GitHub API search/repos（8+6 组关键词，2026-08-29 实测） | 赛道全景、star 数、license |
| 本地浅克隆解剖：`%LOCALAPPDATA%\Temp\kos_cmp\{EverOS,basic-memory,mem0,letta}` | 代码级证据（存储三件套/audit-form/ADD-only/去重机制） |
| EverOS `src/everos/core/persistence/markdown/entries.py` | audit-form=格式非审计（交叉验证） |
| Mem0 `mem0/memory/main.py` L1005-1022, L1152 | hash/语义去重证据 |
| basic-memory `NOTE-FORMAT.md` | md 真相源 + 三元素语法 |
| Y Combinator 公司页（代理抓取） | Mem0 团队/融资背景（YC S24, 10 人, SF） |
| 各项目 README/docs | 商业模式（$15/月/AGPL/Letta Cloud/mem0 三档） |

> 注：本报告为 2026-08-29 快照；AI 记忆赛道月级演化，3 个月后核心数据（尤其 Mem0 架构方向）需复核再引用。
